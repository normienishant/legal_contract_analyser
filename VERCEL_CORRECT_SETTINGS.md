# ✅ Vercel Correct Settings

## Settings to Use:

```
Project Name: legal-contract-analyser (auto - OK)
Vercel Team: normienishant's projects (OK)
Plan: Hobby (Free - OK)

Framework Preset: Next.js ⚠️ Change from "Other"
Root Directory: frontend ✅ (Already correct)

Build Command: npm run build ⚠️ Remove "cd frontend &&"
Output Directory: .next ⚠️ Remove "frontend/"
Install Command: npm install ⚠️ Remove "cd frontend &&"

Environment Variables:
  NEXT_PUBLIC_API_URL = http://localhost:8000
```

---

## How to Edit:

1. **Framework Preset:**
   - Dropdown click → "Next.js" select

2. **Build Command:**
   - Pencil icon (✏️) click
   - Replace with: `npm run build`

3. **Output Directory:**
   - Pencil icon (✏️) click
   - Replace with: `.next`

4. **Install Command:**
   - Pencil icon (✏️) click
   - Replace with: `npm install`

5. **Environment Variables:**
   - Remove `EXAMPLE_NAME` (minus icon click)
   - Add: `NEXT_PUBLIC_API_URL` = `http://localhost:8000`

---

**Edit karo aur Deploy!** 🚀

